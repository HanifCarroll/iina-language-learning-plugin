# Product acceptance evidence

**Status (2026-09-24):** the approved MVP and the 0.1.5 chat update have been tested locally. This is not a release or a claim that all A01–A24 scenarios have passed in IINA. `docs/SPEC.md` remains the behavior contract. [INTEGRATION_MILESTONE.md](INTEGRATION_MILESTONE.md) records earlier disposable-harness results; they are labeled below where relevant and do not prove product behavior.

## Installed IINA chat-label and sidebar check (version 0.1.5)

The sidebar added an `Explanation` label to every assistant turn. The local mock server also prefixed follow-up answers with `**Follow-up**`; together these made the same role labels appear repeatedly. Version 0.1.5 removes the assistant label and the mock-only follow-up heading. The model still controls any headings inside its own answer; the initial prompt asks for Natural and Literal meaning sections.

IINA 1.5.0-beta2 has a host setting at **Video → Show Video Panel → Layout → Sidebar Position → Plugins**. Setting Plugins to the trailing side moved Language Learning to the right in the owner's installed IINA. This setting applies to all plugin sidebars. With the already open Leyla ile Mecnun video, the installed 0.1.5 package displayed one local streamed explanation and one follow-up without either duplicate label. The composer remained at the bottom. Two requests reached only the loopback mock server, with `stream: true` and no Authorization header; no provider call was made. The prior DeepSeek URL, model, key-required state, and saved-key status were restored afterward, and the movie was left paused at 01:22. The screenshot is an ignored local file at `dist/evidence/iina-0.1.5-right-sidebar.png` and contains a frame of the owner's movie; it is not committed.

Checks: `bun test` **43 pass, 0 fail, 229 assertions**; `bun run build` passed TypeScript typechecking and Swift/bundle compilation; packaged helper tests **9 pass**; IINA 0.1.5 archive passed `unzip -t`. `bun run evals .tmp/eval-review.json` still reported **E01–E12 NOT_RUN**, 0/12 passed, because no live model answers have been collected. Automated eval-case tests prove production prompt assembly and grader behavior only.

## Installed IINA dogfood and eval verdicts (version 0.1.4)

Version 0.1.4 was installed through IINA's **Install Local Package** control in the normal profile. The test used the owner's already open `001 - Leyla ile Mecnun 1. Bölüm.webm` in `Movies/Leyla ile Mecnun`. Before any Explain action, the sidebar showed `http://127.0.0.1:47891`, model `synthetic`, and **This endpoint needs no API key** enabled. Three requests reached only the local mock server, each with `stream: true` and no Authorization header. No DeepSeek or other billable request was made. The movie and its subtitle text were not saved as fixtures or committed.

| Native check | Verdict | Observed result |
|---|---|---|
| Install and load 0.1.4 | **PASS** | IINA Plugins listed `IINA Language Learning 0.1.4`; the Language Learning sidebar loaded in the open movie window after selecting it in the Plugins panel. |
| Select and explain an actual subtitle | **PASS** | Double-click selected exactly `halimiz` from the displayed Turkish cue. Explain opened the chat, showed a waiting state, then displayed the local streamed answer. |
| Markdown presentation | **PASS** | Synthetic `**Natural meaning**`, `**Literal meaning**`, and `**Breakdown**` rendered as styled headings rather than raw Markdown markers. This tests presentation, not linguistic quality. |
| Pinned chat composer and growth | **PASS** | A five-line draft kept the composer and controls visible at the bottom; the input showed about three lines and scrolled internally. |
| Chat turns and scrolling | **PASS** | Enter sent the multiline draft as a separate user bubble; a second follow-up appeared below it, and the message pane scrolled to the latest reply while the composer stayed visible. Shift-Enter inserted a newline without sending. |
| Safe close and settings restoration | **PASS** | Close/Resume closed the mock conversation. The original `https://api.deepseek.com` endpoint, `deepseek-flash` model, key-required setting, languages, and secondary-context toggle were restored; the sidebar reported a saved key for that endpoint and “No request was sent.” The movie was returned to 01:22 and left paused. |
| E01–E12 language-quality evals | **NOT RUN** | All 12 synthetic prompts and the binary grader are implemented and checked through the production conversation path. No live model answers were collected, so no linguistic pass/fail is claimed. The ignored review worksheet reports `NOT_RUN` for each case. |

The native screenshot at `dist/evidence/iina-0.1.4-chat.png` shows the second follow-up and pinned composer. It is an ignored local artifact containing a frame from the owner's movie; it is not part of the package or repository. The local mock proves UI and transport behavior on the tested path, not real provider quality. The earlier native subtitle, security, and window limitations in the A01–A24 matrix below still apply.

| Current check | Result |
|---|---|
| `bun test` | 43 pass, 0 fail, 226 assertions across 10 files, including 14 eval checks. |
| `bun run build` | Passed `tsc --noEmit`, Swift compilation, and plugin bundling. |
| `python3 tests/test_stream_helper.py dist/io.github.hanifcarroll.iina-language-learning.iinaplugin/native/stream-helper` | 9 pass. |
| IINA plugin packer and `unzip -t` | 0.1.4 archive created; all members passed integrity. |
| `bun run evals .tmp/eval-review.json` | E01–E12 each `NOT_RUN`, 0/12 passed, exit 1 as designed until completed model answers are reviewed. |

## UI dogfood update (version 0.1.2)

The owner's live IINA screenshot and a read-only inspection of that open sidebar showed three problems: the phrase and nearly identical full cue took two lines of prominent space, provider Markdown appeared as raw `**` text, and a long answer pushed the follow-up composer below the visible area. Version 0.1.2 formats model Markdown locally with HTML, links, and image loading disabled; keeps the full cue behind **Full subtitle line**; pins the phrase and composer around a scrollable answer; and preserves completed answer DOM nodes across status updates. No new provider request was made for this review.

The revised sidebar was visually checked in a 470 px browser preview with synthetic completed, empty-streaming, long-answer, and settings states. The long-answer check found an initial auto-scroll to the bottom; that was fixed and the top and scrolled states were rechecked. The browser preview was layout evidence only. At that time the normal IINA installation and its existing movie/credentials were untouched; the later 0.1.4 installed-IINA check is recorded above. The version 0.1.0 native evidence and the original 26-test result below remain historical.

For 0.1.2, `bun test` passed 28 tests with 129 assertions, `bun run build` passed TypeScript typechecking and bundling, and IINA's packer created an archive that passed `unzip -t`. The archive contains the Swift helper and bundled Markdown dependency notices. A preview file was briefly present in the build folder during visual testing; the build now clears that folder before packaging, and the final archive was checked to exclude it.

## Chat UX update and proposed evals (version 0.1.3)

The composer now starts at one line, grows to three, and scrolls internally after that. Follow-up questions appear as separate user messages. A new follow-up scrolls the message list to the bottom; streamed text follows only while the reader is near the bottom. The composer and its controls remain visible below the message list. Sending while a response is active keeps the draft and starts no second request. Enter, Shift+Enter, and composition handling remain distinct.

DOM tests cover growth, the three-line cap, new-turn scrolling, manual-scroll preservation, busy-send rejection, and inert user-message text. A synthetic browser preview at 470 px and 320 px showed the composer at the bottom; typing four lines left the input at three visible lines, and Enter displayed the new question above a streaming reply. This is browser layout evidence, not an installed-IINA test. The normal IINA configuration and real provider were untouched.

The linguistic evaluation set and scoring rubric are in [docs/EVAL_PLAN.md](docs/EVAL_PLAN.md). No provider evaluation has been run, and the Turkish reference notes still need language review before use as a benchmark.

For 0.1.3, `bun test` passed 29 tests with 144 assertions, and `bun run build` passed TypeScript typechecking, Swift compilation, and bundling. IINA's packer created the 0.1.3 archive; `unzip -t` passed, and its member list includes the Swift helper and Markdown notices but no browser preview file. That package was subsequently installed for the native chat check before the 0.1.4 input-height adjustment.

## Environment and levels of evidence

- **Installed product:** version 0.1.0 was installed through IINA's **Install Local Package** control in a fresh isolated profile at `/tmp/iina-final-install-isolated`. IINA 1.5.0-beta2 build 172 restarted and loaded that installed copy on macOS 27 arm64 with bundled mpv 0.41.0. The permission dialog showed the documented `file-system` and `video-overlay` permissions. At that point the normal IINA profile was not modified. Version 0.1.4 later loaded in the owner's already open movie window after a local reinstall and plugin reload; its sidebar and chat were checked above.
- **Earlier product folder:** the same product architecture was exercised through IINA's development link in `/tmp/iina-product-isolated`. A later code fix changed the Stop status guard and the `event.off(name, id)` teardown call; those fixes passed automated tests, and the final installed archive loaded without the earlier listener warning. Results from that folder are identified separately.
- **Automated:** Bun runs pure TypeScript and DOM/bridge/session tests. The Python tests execute the final compiled Swift helper against local controlled endpoints. All fixtures and media are synthetic. No real provider key, billable request, or commercial subtitle file was used.

## Earlier implementation commands

| Check | Result |
|---|---|
| `bun test` | 26 passed, 0 failed, 116 assertions across nine files. Covers parser/timing/context, selection drag and seek, DOM safety, conversation ownership, Keychain adapter, stdout queue, session lifecycle, settings, and late installation into an open player. |
| `bun run build` | Passed actual `tsc --noEmit`, Swift compilation, and three Bun bundles; built one `.iinaplugin` package. |
| `python3 tests/test_stream_helper.py dist/io.github.hanifcarroll.iina-language-learning.iinaplugin/native/stream-helper` | 9 passed. Real delayed SSE, auth and same-origin redirect, Stop, closed stdout, timeouts, interruption, boundary errors, TLS rejection, and FIFO cleanup. |
| `/Applications/IINA.app/Contents/MacOS/iina-plugin pack dist/io.github.hanifcarroll.iina-language-learning.iinaplugin` and `unzip -t` | Final `.iinaplgz` archive packed and every member, including `LICENSE` and the Swift executable, passed integrity check. The archive is ignored by Git. |
| Installed IINA UI | Archive installed in a fresh isolated profile; restart loaded the plugin, external synthetic WebVTT was selectable, missing settings failed visibly without a request, settings saved a keyless local endpoint without a request, and Explain streamed and completed against a local mock server. |

## A01–A24 acceptance matrix

“Automated” means a unit/DOM/helper check. “Native” means observed in installed IINA with synthetic media. “Earlier product” and “harness” identify distinct builds. Partial rows list the remaining check; no row inherits proof from another level.

| ID | Evidence and current result |
|---|---|
| A01 | **Partial.** Installed 0.1.4 selected exact `halimiz` in the owner's Turkish movie subtitle and sent one local mock request only after Explain; sidebar opened and answer completed. Exact `mıyım` word test and request-body capture in native IINA remain. Automated session test verifies exact frozen phrase/cue. |
| A02 | **Partial.** Earlier harness selected exact phrases across a visual wrap with real pointer drags. Product DOM test verifies selection survives repaint; final installed product phrase selection worked. Native final-product drag across a wrap remains. |
| A03 | **Pass on tested paths.** Earlier product folder and harness showed selection alone left playback and network untouched; outside-area click reached IINA. Final installed Explain required the action button. Automated forged/empty selection checks send nothing. Alternate display scales remain untested. |
| A04 | **Automated pass, native partial.** Separate selection and session tests verify natural cue advancement freezes pending text, including during an active drag; user seek clears uninvoked text; an active conversation retains its original context. The earlier harness's cue change followed a seek and cannot prove this distinction. Final-product native mid-drag cue transition remains. |
| A05 | **Automated pass.** Full external SRT/VTT timeline gives three existing cues before/after in order before the following cues play; boundaries return fewer. Earlier IINA Phase 0 `@sub` read confirmed future SRT text, and final installed product read external WebVTT. Full-length real user files were not tested. |
| A06 | **Automated pass, native partial.** Tests cover repeated text, overlap, seek, source/secondary delay and `sub-speed` calculations; displayed text/start mismatch restores native primary and blocks selection. Phase 0 native source-delay behavior was verified. Native final-product `sub-speed`, secondary delay, and malformed-track cases remain. |
| A07 | **Pass on tested native paths, with approved host limitation.** Earlier product folder displayed selectable Turkish once beside native English and, during an active local stream, Disable Overlay closed/resumed that window, restored native Turkish, stopped the helper, and emptied its private request directory. Session test rejects late chunks and leaves the other window untouched. Earlier harness tested raw Preferences disable and manual **Subtitles → Show Subtitles** / **Playback → Resume** recovery. Raw disable can leave a helper running to its 120-second total timeout, native primary hidden, playback paused, and a provider charge possible. No immediate cancellation or billing prevention is promised. |
| A08 | **Automated pass, native partial.** Context assembly includes overlapping secondary cues only when enabled; settings changes close the current conversation, while secondary visibility is never changed. Native dual display passed in earlier product. Native toggle-and-new-request comparison remains. |
| A09 | **Prompt and presentation pass; linguistic quality outstanding.** Automated test requires natural and literal meaning and grammar/breakdown in the initial message. Installed 0.1.4 rendered the local mock's Markdown headings. No real model was called and no linguistic quality claim is made. |
| A10 | **Pass on tested paths.** Installed 0.1.4 sent two follow-ups through the local mock and displayed separate user/assistant turns with a pinned composer. Automated test verifies frozen selection/context and completed prior turns. Question text is bounded. |
| A11 | **Automated pass, native partial.** New Explain invalidates the old request and late chunks cannot enter the new conversation; earlier product UI no longer exposes stale Explain action after a conversation opens. Native two-request race remains. |
| A12 | **Pass on tested paths.** Installed 0.1.4 showed a waiting state and completed through the packaged helper against a delayed local SSE endpoint; earlier installed evidence captured a partial response before completion. Compiled helper tests fragment SSE/UTF-8 and exercise Stop and timeout. `utils.exec` stdout hook only queues bytes; the 100 ms plugin timer performs host/UI/file work. This tested restriction does not eliminate every host-level concurrency risk. |
| A13 | **Pass on tested paths.** Earlier product Stop during partial output showed incomplete, Retry completed, and native Back/Close resumed eligible playback. Final installed helper and session tests cover cancellation and late-result invalidation. A Stop after completion now leaves completed status intact. |
| A14 | **Partial.** Earlier harness and product showed native Back, outside click, tab switch, fullscreen Back, explicit Close/Resume, minimize, and Settings behavior. Automated test distinguishes settings dismissal from conversation dismissal and blocks resume after media replacement. Dedicated app-focus and final-product ended-media close still need native checks. |
| A15 | **Automated pass, native partial.** Two fake player sessions keep helper/visibility/resume ownership separate; replacement cancels and rejects old output. Earlier product had two native windows with distinct conversations, and closing one resumed only that window. A final installed attempt to open another file during a stream opened a **new** IINA window; the stream completed while the file chooser was open, so it is not evidence of active media replacement. Simultaneous native requests and same-window media replacement remain. |
| A16 | **Pass on tested paths.** Final installed product showed a missing-settings failure and no helper request. Saving a keyless loopback endpoint displayed the exact Chat Completions URL and “No request was sent.” Automated validation rejects invalid URL/model/credential setup. |
| A17 | **Partial.** Installed IINA Phase 0 wrote, read, replaced and read after restart using exact lowercase `keychainWrite`/`keychainRead`. Adapter tests reject write failure without plaintext fallback and bind status to endpoint. Final product saved only a keyless mock endpoint; a forced same-item Keychain overwrite failure and final-product restart with a disposable key remain. Saved keys are never returned to UI state. |
| A18 | **Automated pass, native partial.** Credential adapter binds keys to canonical endpoints; settings change closes old conversation, and a new endpoint cannot inherit its key. A native endpoint-switch test with two disposable local servers remains. |
| A19 | **Helper pass, UI partial.** Compiled helper tests covered 401/403/429/503, malformed SSE, interrupted partial stream, first-byte/idle/total timeouts, same-origin 307/308 auth preservation, cross-origin/302 rejection, and rejection of an untrusted self-signed TLS certificate. It uses system TLS validation. A positive request to a trusted controlled HTTPS server, product UI for each error, and provider-specific response variants remain. No browser certificate warning was bypassed. |
| A20 | **Automated pass, native partial.** DOM uses plain text and tests inert model/subtitle HTML. Forged bridge selection cannot start a request or pause; no raw model HTML, remote image loading, or arbitrary privileged command route exists. Native malicious-text fixture and exhaustive prompt-injection behavior remain. |
| A21 | **Partial.** Parser rejects malformed/ambiguous cues; source mismatch restores native primary with a visible status. Final IINA with no source retained native playback. Native unreadable file, bitmap/embedded, and empty-cue cases remain. |
| A22 | **Partial.** Earlier harness tested wrapped drag, pointer pass-through, dual display, fullscreen Back, and separate windows; earlier product tested fullscreen selection. Installed 0.1.4 selected a word in the movie, kept the composer at the bottom, limited its visible growth to three lines, accepted Shift-Enter, and scrolled new chat turns into view. Product keyboard subtitle selection, native app-focus dismissal, resize/display-scale coverage remain. |
| A23 | **Automated pass, native partial.** Media change and teardown clear in-memory conversation and request ownership; only non-secret preferences and Keychain status persist. Installed archive loaded after restart. A final-product native chat-then-restart check remains. |
| A24 | **Automated pass, native partial.** Parsers, request/body, cue, question, turn, answer, and SSE limits fail visibly; required context is not silently dropped. Native oversize-file and maximum-history UI checks remain. |

## Current limits and recovery

IINA installed the plugin into an already open movie window in the normal profile at 21:37, after that window's `iina.window-loaded` event. Version 0.1.0 waited for the missed event and showed an empty sidebar tab. IINA source confirms a newly enabled plugin instance is added to existing players without replaying that event. Fully quitting and reopening IINA is the immediate recovery. Version 0.1.1 checks `core.window.loaded`, initializes that existing window once, and has an automated test for both late install and duplicate event delivery. Native hot-install of 0.1.1 remains unverified.

The tested IINA release has no plugin teardown callback on raw Preferences disable. Use the in-plugin **Disable Overlay** control for safe shutdown. If raw disable hides source text, use **Subtitles → Show Subtitles**; if a conversation left playback paused, use **Playback → Resume**. The helper may continue to its total timeout and an external provider may bill for work already sent. Quit IINA disconnected the tested helper and cleaned its FIFO, but forced host crashes and exact cancellation latency are not guaranteed.

The plugin intentionally requires user-supplied external UTF-8 SRT/VTT for selectable source subtitles. Full-length files are supported within the documented 8 MiB/50,000-cue limits; complete commercial tracks are valid runtime input but never included as fixtures. Embedded, bitmap, or unreadable tracks keep IINA's native playback usable. Any positive HTTPS and real-model quality review needs a controlled trusted endpoint or later explicit billable-call authorization, respectively. This local work has not been pushed or released.
